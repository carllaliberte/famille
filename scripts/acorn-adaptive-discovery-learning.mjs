export const CONTRACT="acorn.adaptive-discovery-learning.v1";
export const CYCLE=Object.freeze(["OBSERVE","FIND_UNKNOWN","GENERATE_HYPOTHESES","DESIGN_EXPERIMENT","SIMULATE","TEST","MEASURE","VERIFY","COMPARE","LEARN","UPDATE_MODEL","REOBSERVE"]);
const A=v=>Array.isArray(v)?v:[]; 
export function discoverUnknowns({known=[],observations=[],failures=[],gaps=[]}={}){const knownSet=new Set(A(known));const candidates=[...A(gaps),...A(failures)].filter(Boolean).filter(x=>!knownSet.has(x));return {unknowns:[...new Set(candidates)],observations:A(observations),authority:false,auto_execute:false};}
export function buildLearningExperiment({hypothesis,baseline=null,variables=[],constraints=[]}={}){return {contract:CONTRACT,hypothesis:hypothesis??null,baseline,variables:A(variables),constraints:A(constraints),simulation_required:true,retest_required:true,reverification_required:true,authority:false,auto_adopt:false};}
export function learnFromOutcome({outcome={},model={}}={}){const valid=outcome?.measured===true&&outcome?.verified===true;return {state:valid?"LEARNED":"HOLD_FOR_EVIDENCE",model:valid?{...model,last_outcome:outcome.id??null,revision:(Number(model.revision)||0)+1}:model,authority:false};}
export function assertAdaptiveLearningConstitution(x={}){const violations=[];if(x.hidden_learning===true)violations.push("HIDDEN_LEARNING");if(x.auto_adopt===true)violations.push("AUTO_ADOPTION");if(x.auto_execute===true)violations.push("AUTO_EXECUTION");if(x.authority===true)violations.push("AUTHORITY");return {contract:CONTRACT,valid:!violations.length,violations};}
