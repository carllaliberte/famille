/** ACORN MEGA 073 — Cognitive Attention Economy. */
export const CONTRACT='acorn.mega.attention-economy.v1';const A=v=>Array.isArray(v)?v:[];const N=v=>Number.isFinite(Number(v))?Number(v):0;
export function createAttention2({id,label,allocation=0,evidence=[],source='unknown'}={}){return {contract:CONTRACT,id:String(id||'attention2-'+Date.now()),label:String(label||'UNKNOWN'),allocation:N(allocation),evidence:A(evidence),source:String(source),state:'PROPOSED',measured:false,verified:false,authority:false,live:false};}
export function measureAttention2(rows=[]){const records=A(rows).map(r=>({...r,measured:true,measurement:{allocation:N(r.allocation)},authority:false,live:false}));return {contract:CONTRACT,records,count:records.length,verified_count:records.filter(r=>r.verified===true&&r.evidence.length>0).length,authority:false,live:false};}
export function routeAttention2(rows=[]){return A(rows).filter(r=>r.state!=='REVOKED'&&r.state!=='EXPIRED').sort((a,b)=>N(b.allocation)-N(a.allocation)).slice(0,8);}
export function evolveAttention2(row,{delta=0,verified=false}={}){return verified?{...row,allocation:N(row.allocation)+N(delta),state:'MEASURED_CANDIDATE',authority:false}:{...row,state:'HUMAN_REVIEW_REQUIRED',authority:false};}
export function assertConstitution(x={}){const v=[];for(const [k,n] of [['authority','AUTHORITY_ESCALATION'],['auto_authorize','AUTO_AUTHORIZATION'],['auto_execute','AUTO_EXECUTION'],['fake_live','FAKE_LIVE'],['breaker_bypass','BREAKER_BYPASS']])if(x[k]===true)v.push(n);return {contract:CONTRACT,valid:v.length===0,violations:v};}
export function selfTest(){const r=createAttention2({id:'test',allocation:1,evidence:['e']});return measureAttention2([r]).count===1&&assertConstitution({}).valid;}
