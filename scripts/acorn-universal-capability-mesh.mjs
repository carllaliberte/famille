/** ACORN — UNIVERSAL CAPABILITY MESH v2: evidence-bound routing. */
export const CONTRACT="acorn.universal-capability-mesh.v2";
export const STATES=Object.freeze(["DECLARED","DISCOVERED","QUALIFIED","ROUTABLE","DEGRADED","EXPIRED","REVOKED","BLOCKED"]);
export const CYCLE=Object.freeze(["DECLARE","DISCOVER","NORMALIZE","QUALIFY","MEASURE","ROUTE","COMPOSE","EXECUTE_THROUGH_GOVERNANCE","OBSERVE","VERIFY","LEARN","EXPIRE_OR_REVOKE","REDISCOVER"]);
const A=v=>Array.isArray(v)?v:[]; const s=v=>String(v??"").trim(); const finite=v=>Number.isFinite(Number(v))?Number(v):null;
export function declareCapability({id,kind="UNKNOWN",name=null,provider=null,version="1",inputs=[],outputs=[],constraints=[],evidence=[],authority=false}={}){return {contract:CONTRACT,id:s(id)||null,kind:s(kind)||"UNKNOWN",name:name??id??null,provider:provider??null,version:s(version)||"1",inputs:A(inputs),outputs:A(outputs),constraints:A(constraints),evidence:A(evidence),state:"DECLARED",authority:false,declared_authority:authority===true,live:false};}
export function qualifyCapability(capability,{verified=false,measured=false,evidence=[]}={}){const ev=[...A(capability?.evidence),...A(evidence)];const qualified=verified===true&&measured===true&&ev.length>0;return {...capability,evidence:ev,state:qualified?"QUALIFIED":"DISCOVERED",qualified,measured,verified,authority:false,live:false};}
export function scoreCapability(capability,{context={},outcomes=[]}={}){const rows=A(outcomes).filter(x=>x?.measured===true&&x?.verified===true&&x?.capability_id===capability?.id);const reliability=rows.length?rows.filter(x=>x.success===true).length/rows.length:null;const costs=rows.map(x=>finite(x.cost)).filter(x=>x!==null),lat=rows.map(x=>finite(x.latency)).filter(x=>x!==null);return {capability_id:capability?.id??null,context,observed_outcomes:rows.length,reliability,cost:costs.length?costs.reduce((a,b)=>a+b,0)/costs.length:null,latency:lat.length?lat.reduce((a,b)=>a+b,0)/lat.length:null,evidence_bound:true,optimization:"BEST_KNOWN_IN_SCOPE",authority:false};}
export function routeCapabilities({capabilities=[],required=[],context={},outcomes=[]}={}){const need=new Set(A(required).map(s));return A(capabilities).filter(c=>c?.state==="QUALIFIED"&&c?.live===true&&(!need.size||need.has(s(c.id))||need.has(s(c.name)))).map(c=>({...c,score:scoreCapability(c,{context,outcomes}),routeable:true})).sort((a,b)=>(b.score.reliability??-1)-(a.score.reliability??-1)||s(a.id).localeCompare(s(b.id)));}
export function composeCapabilities({capabilities=[],requirements=[],context={},outcomes=[]}={}){const routes=routeCapabilities({capabilities,required:requirements,context,outcomes});return {contract:CONTRACT,requirements:A(requirements),context,candidates:routes,composition:routes.map(x=>x.id),state:routes.length?"COMPOSITION_CANDIDATE":"NO_CAPABILITY",requires_governance:true,authority:false,external_effect:false,auto_execute:false};}
export function learnCapability({capability,outcome}={}){const valid=outcome?.measured===true&&outcome?.verified===true&&outcome?.capability_id===capability?.id;return {...capability,learning:valid?{state:"LEARNED",outcome_id:outcome.id??null}:{state:"HOLD_FOR_EVIDENCE"},authority:false};}
export function expireCapability(capability,{expired=false,revoked=false,reason=null}={}){return {...capability,state:revoked?"REVOKED":expired?"EXPIRED":capability?.state??"DECLARED",expiry_reason:reason,authority:false};}
export function buildCapabilityMesh({capabilities=[],context={},outcomes=[],requirements=[]}={}){const qualified=A(capabilities).map(c=>c?.state==="QUALIFIED"?c:qualifyCapability(c,{verified:c?.verified===true,measured:c?.measured===true}));const routes=routeCapabilities({capabilities:qualified,required:requirements,context,outcomes});return {contract:CONTRACT,cycle:CYCLE,capabilities:qualified,routes,composition:composeCapabilities({capabilities:qualified,requirements,context,outcomes}),counts:{declared:qualified.length,qualified:qualified.filter(x=>x.state==="QUALIFIED").length,routable:routes.length},authority:false,external_effect:false,auto_authorize:false,auto_execute:false,live:false};}
export function assertCapabilityMeshConstitution(snapshot={}){const violations=[];for(const [k,msg] of [["authority","CAPABILITY_AUTHORITY_FORBIDDEN"],["auto_authorize","AUTO_AUTHORIZATION"],["auto_execute","AUTO_EXECUTION"],["external_effect","EXTERNAL_EFFECT"],["breaker_bypass","BREAKER_BYPASS"],["hidden_learning","HIDDEN_LEARNING"],["unverified_live","UNVERIFIED_LIVE"]])if(snapshot[k]===true)violations.push(msg);return {contract:CONTRACT,valid:!violations.length,violations,capability_ne_authority:true,existing_governance_only:true};}
