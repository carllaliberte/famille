/** ACORN MEGA 071 — Unknown-Space Hypothesis Explorer. */
export const CONTRACT='acorn.mega.hypothesis-exploration.v1';const A=v=>Array.isArray(v)?v:[];const N=v=>Number.isFinite(Number(v))?Number(v):0;
export function createExplorer({id,label,information=0,evidence=[],source='unknown'}={}){return {contract:CONTRACT,id:String(id||'explorer-'+Date.now()),label:String(label||'UNKNOWN'),information:N(information),evidence:A(evidence),source:String(source),state:'PROPOSED',measured:false,verified:false,authority:false,live:false};}
export function measureExplorer(rows=[]){const records=A(rows).map(r=>({...r,measured:true,measurement:{information:N(r.information)},authority:false,live:false}));return {contract:CONTRACT,records,count:records.length,verified_count:records.filter(r=>r.verified===true&&r.evidence.length>0).length,authority:false,live:false};}
export function routeExplorer(rows=[]){return A(rows).filter(r=>r.state!=='REVOKED'&&r.state!=='EXPIRED').sort((a,b)=>N(b.information)-N(a.information)).slice(0,8);}
export function evolveExplorer(row,{delta=0,verified=false}={}){return verified?{...row,information:N(row.information)+N(delta),state:'MEASURED_CANDIDATE',authority:false}:{...row,state:'HUMAN_REVIEW_REQUIRED',authority:false};}
export function assertConstitution(x={}){const v=[];for(const [k,n] of [['authority','AUTHORITY_ESCALATION'],['auto_authorize','AUTO_AUTHORIZATION'],['auto_execute','AUTO_EXECUTION'],['fake_live','FAKE_LIVE'],['breaker_bypass','BREAKER_BYPASS']])if(x[k]===true)v.push(n);return {contract:CONTRACT,valid:v.length===0,violations:v};}
export function selfTest(){const r=createExplorer({id:'test',information:1,evidence:['e']});return measureExplorer([r]).count===1&&assertConstitution({}).valid;}
