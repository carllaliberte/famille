import {collectRuntimeEvidence,activateMeasuredCapability,buildRuntimeReobservation} from "./acorn-runtime-evidence-activation.mjs";
export const CONTRACT="acorn.runtime-activation-conductor.v1";
export function runRuntimeActivation({runs=[],connectors=[],capabilities=[],outcomes=[],gaps=[]}={}){const evidence=collectRuntimeEvidence({runs,connectors,capabilities,outcomes}),candidates=capabilities.map(cap=>activateMeasuredCapability({capability:cap,connector:connectors.find(c=>c.state==="LIVE_VERIFIED")||{},outcome:outcomes.find(o=>o?.verified===true&&o?.measured===true)||{}})).filter(x=>x.state==="ACTIVATION_CANDIDATE"),reobserve=buildRuntimeReobservation({gaps});return {contract:CONTRACT,evidence,activation_candidates:candidates,reobserve,human_gate:true,authority:false,auto_authorize:false,auto_execute:false,live:false}}
export function assertRuntimeActivationConstitution(x={}){const v=[];if(x.auto_authorize===true)v.push("AUTO_AUTHORIZATION");if(x.auto_execute===true)v.push("AUTO_EXECUTION");if(x.authority===true)v.push("AUTHORITY_ESCALATION");return {contract:CONTRACT,valid:!v.length,violations:v}}
