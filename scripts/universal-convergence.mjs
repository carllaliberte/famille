export const CONTRACT="acorn.universal-convergence.v2";export const CHECKS=["DUPLICATES","DEAD_LOOPS","UNCONNECTED_CAPABILITIES","FAKE_LIVE","UNGOVERNED_EFFECTS","RESOURCE_WASTE","SECURITY_GAPS","ECONOMIC_INCONSISTENCIES","UNSUPPORTED_COMPONENTS"];export function audit({capabilities=[],loops=[],connectivity=[],effects=[],resources=[],security=[],economics=[],components=[]}={}){const f=[];if(capabilities.some(x=>!x.routeable))f.push("UNCONNECTED_CAPABILITIES");if(connectivity.some(x=>x.state==="LIVE_VERIFIED"&&!x.evidence?.length))f.push("FAKE_LIVE");if(effects.some(x=>x.governed!==true))f.push("UNGOVERNED_EFFECTS");if(resources.some(x=>x.state==="EXHAUSTED"))f.push("RESOURCE_WASTE");if(security.some(x=>x.state==="DEGRADED"))f.push("SECURITY_GAPS");if(economics.some(x=>x.consistent===false))f.push("ECONOMIC_INCONSISTENCIES");if(components.some(x=>x.supported!==true))f.push("UNSUPPORTED_COMPONENTS");if(loops.some(x=>x.dead===true))f.push("DEAD_LOOPS");return{contract:CONTRACT,checks:CHECKS,findings:[...new Set(f)],state:f.length?"REMEDIATION_REQUIRED":"CONVERGED",authority:false,auto_execute:false};}export function closure(a){return{contract:CONTRACT,state:a.findings?.length?"CLOSURE_CANDIDATES":"STABLE_MONITORING",findings:a.findings||[],human_authorization_required:true,authority:false};}export function assertConvergenceConstitution(x={}){const violations=["authority","auto_execute","fake_live","ungoverned_effect"].filter(k=>x[k]===true);return{contract:CONTRACT,valid:!violations.length,violations};}