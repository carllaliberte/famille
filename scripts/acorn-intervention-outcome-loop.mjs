export const CONTRACT="acorn.intervention-outcome-loop.v1";
export function recordInterventionOutcome({action={},before={},after={},evidence=[]}={}){const measured=evidence.some(e=>e?.measured===true),verified=evidence.some(e=>e?.verified===true),delta={};for(const k of new Set([...Object.keys(before),...Object.keys(after)])){if(typeof before[k]==="number"&&typeof after[k]==="number")delta[k]=after[k]-before[k]}return {contract:CONTRACT,action,before,after,delta,evidence_count:evidence.length,measured,verified,state:measured&&verified?"VERIFIED_OUTCOME":"OUTCOME_REQUIRES_EVIDENCE",authority:false,auto_execute:false,live:false}}
export function evaluateInterventionOutcome({outcome={},targets={}}={}){const comparisons=Object.fromEntries(Object.entries(targets).map(([k,v])=>[k,{target:v,observed:outcome.after?.[k],delta:outcome.delta?.[k]}]));return {contract:CONTRACT,comparisons,decision:outcome.measured&&outcome.verified?"RETAIN_OR_REVIEW":"REOBSERVE",human_gate:true,authority:false,auto_adopt:false,auto_execute:false}}
export function buildInterventionLearning({outcomes=[]}={}){const valid=outcomes.filter(o=>o.state==="VERIFIED_OUTCOME");return {contract:CONTRACT,verified_outcomes:valid.length,learning:valid.length?"LEARNING_CANDIDATE":"INSUFFICIENT_EVIDENCE",next:"RETEST_OR_REOBSERVE",authority:false,auto_adopt:false}}
export function assertInterventionOutcomeConstitution(x={}){const v=[];if(x.authority===true)v.push("AUTHORITY_ESCALATION");if(x.auto_adopt===true)v.push("AUTO_ADOPT");if(x.auto_execute===true)v.push("AUTO_EXECUTION");if(x.live===true)v.push("FAKE_LIVE");return {contract:CONTRACT,valid:!v.length,violations:v}}
