export const CONTRACT="acorn.continuous-runtime-control-loop.v1";
const verified=x=>x?.measured===true&&x?.verified===true&&x?.evidence;
export function assessRuntimeState({runs=[],connectors=[],capabilities=[],outcomes=[],gaps=[]}={}){const successful=runs.filter(x=>x?.status==="SUCCEEDED"&&x?.evidence),live=connectors.filter(x=>x?.state==="LIVE_VERIFIED"&&x?.evidence),qualified=capabilities.filter(verified),proven=outcomes.filter(verified);return {contract:CONTRACT,successful_runs:successful.length,live_connectors:live.length,qualified_capabilities:qualified.length,verified_outcomes:proven.length,gaps:gaps.length,state:gaps.length?"GAPS_PRESENT":"REOBSERVE_REQUIRED",truth:"CONTINUOUS_RUNTIME_STATE_REQUIRES_REPEATED_EVIDENCE",authority:false,auto_authorize:false,auto_execute:false,live:false}}
export function scheduleRuntimeCycle({state={},interval="event"}={}){return {contract:CONTRACT,cycle:["OBSERVE","INGEST_EVIDENCE","ASSESS","COMPARE","DETECT_DRIFT","SELECT_REOBSERVATION","REQUEST_AUTHORIZATION","ACT_THROUGH_GOVERNANCE","OBSERVE_AGAIN"],trigger:interval,requires_human_gate:true,authority:false,auto_execute:false,live:false}}
export function detectRuntimeDrift({baseline={},current={}}={}){const keys=["successful_runs","live_connectors","qualified_capabilities","verified_outcomes"];return {contract:CONTRACT,changes:keys.filter(k=>baseline[k]!==undefined&&current[k]!==baseline[k]).map(k=>({metric:k,baseline:baseline[k],current:current[k]})),state:"DRIFT_MEASURED",truth:"DRIFT_IS_AN_OBSERVATION_NOT_A_CAUSAL_EXPLANATION"}}
export function assertContinuousRuntimeConstitution(x={}){const v=[];if(x.authority===true)v.push("AUTHORITY_ESCALATION");if(x.auto_authorize===true)v.push("AUTO_AUTHORIZATION");if(x.auto_execute===true)v.push("AUTO_EXECUTION");if(x.live===true)v.push("FAKE_LIVE");return {contract:CONTRACT,valid:!v.length,violations:v}}
