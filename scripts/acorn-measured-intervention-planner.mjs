export const CONTRACT="acorn.measured-intervention-planner.v1";
const evidence=x=>x?.measured===true&&x?.verified===true&&x?.evidence;
export function identifyInterventions({gaps=[],drift=[],opportunities=[]}={}){const items=[...gaps.map((g,i)=>({id:"gap-"+i,type:"GAP_CLOSURE",target:g,priority:5,requires_evidence:true})),...drift.map((d,i)=>({id:"drift-"+i,type:"DRIFT_REVIEW",target:d.metric,priority:4,requires_evidence:true})),...opportunities.map((o,i)=>({id:"opportunity-"+i,type:o.type||"OPPORTUNITY",target:o.target||o,priority:o.priority||3,requires_evidence:true}))];return {contract:CONTRACT,interventions:items.sort((a,b)=>b.priority-a.priority).slice(0,50),state:items.length?"INTERVENTIONS_IDENTIFIED":"NO_INTERVENTION_MEASURED",authority:false,auto_authorize:false,auto_execute:false,live:false}}
export function buildInterventionPlan({intervention={},resources=[],evidenceSet=[]}={}){const supported=evidenceSet.filter(e=>evidence(e)),capacity=resources.filter(r=>r?.state==="AVAILABLE"||r?.state==="RESERVED");return {contract:CONTRACT,intervention,steps:["BASELINE","HYPOTHESIS","SIMULATE","EXECUTE_THROUGH_GOVERNANCE","OBSERVE","MEASURE","VERIFY","RETAIN_OR_ROLLBACK"],supporting_evidence:supported.length,available_resources:capacity.length,requires_human_authorization:true,rollback_required:true,auto_execute:false}}
export function compareInterventionPlans({plans=[]}={}){return {contract:CONTRACT,plans:plans.map(p=>({id:p.id,supporting_evidence:p.supporting_evidence||0,available_resources:p.available_resources||0,rollback_required:p.rollback_required!==false})),selection:"HUMAN_OR_MEASURED_GATE",authority:false,auto_select:false,auto_execute:false}}
export function assertInterventionConstitution(x={}){const v=[];if(x.authority===true)v.push("AUTHORITY_ESCALATION");if(x.auto_authorize===true)v.push("AUTO_AUTHORIZATION");if(x.auto_execute===true)v.push("AUTO_EXECUTION");if(x.auto_select===true)v.push("AUTO_SELECTION");if(x.live===true)v.push("FAKE_LIVE");return {contract:CONTRACT,valid:!v.length,violations:v}}
