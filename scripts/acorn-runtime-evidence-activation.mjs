export const CONTRACT="acorn.runtime-evidence-activation.v1";
const verified=x=>x?.measured===true&&x?.verified===true&&x?.evidence;
export function collectRuntimeEvidence({runs=[],connectors=[],capabilities=[],outcomes=[]}={}){const live=connectors.filter(x=>x?.state==="LIVE_VERIFIED"&&x?.evidence),goodCaps=capabilities.filter(verified),goodOutcomes=outcomes.filter(verified),successfulRuns=runs.filter(x=>x?.status==="SUCCEEDED"&&x?.evidence);return {contract:CONTRACT,runtime_runs:successfulRuns.length,live_connectors:live.length,qualified_capabilities:goodCaps.length,verified_outcomes:goodOutcomes.length,state:successfulRuns.length||live.length?"RUNTIME_EVIDENCE_PRESENT":"RUNTIME_EVIDENCE_GAP",truth:"RUNTIME_EVIDENCE_IS_MEASURED_STATE_NOT_PERMANENT_TRUTH",authority:false,live:false,auto_execute:false}}
export function activateMeasuredCapability({capability={},connector={},outcome={}}={}){const ready=verified(capability)&&connector?.state==="LIVE_VERIFIED"&&connector?.evidence&&verified(outcome);return {contract:CONTRACT,state:ready?"ACTIVATION_CANDIDATE":"BLOCKED_INSUFFICIENT_EVIDENCE",requires_existing_governance:true,human_authorization_required:true,authority:false,auto_execute:false,live:false}}
export function buildRuntimeReobservation({gaps=[]}={}){return {contract:CONTRACT,targets:gaps.length?gaps:["runtime_execution","connector_reality","capability_outcomes"],requires_measurement:true,requires_human_gate:true,auto_execute:false}}
export function assertRuntimeEvidenceConstitution(x={}){const v=[];if(x.authority===true)v.push("AUTHORITY_ESCALATION");if(x.auto_execute===true)v.push("AUTO_EXECUTION");if(x.live===true)v.push("FAKE_LIVE");return {contract:CONTRACT,valid:!v.length,violations:v}}
