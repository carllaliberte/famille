# Nerve — circulation overlay

Couche transversale. Pas un deuxième mesh. Pas un deuxième mode. Pas un juge.

Chaîne (extensible) :

SOURCE → SIGNAL → SYNAPSE → ROUTAGE → CONTEXTE → COGNITION → CONTRE-ANALYSE → EVIDENCE → MESURE → PROVENANCE → MÉMOIRE → GOUVERNANCE → DÉCISION HUMAINE → ACTION → OBSERVATION → FEEDBACK → RÉÉVALUATION → SIGNAL → ∞

∞ = horizon ouvert. Pas omniscience. Pas système terminé.

## Ce qui existe déjà (ne pas reconstruire)

| Voie | Où |
|---|---|
| Identités | `schema/agents.json` |
| Enveloppes | `schema/mesh.v0.json` |
| Mode cognitif | `COLLECTIVE_COGNITION` + `schema/cognition.v0.json` |
| Session | `schema/pool.v0.json` |
| Juge / flux | `juge.v0` / `flux.v0` — intouchés |
| Canal runtime | commentaires PR + `.github/workflows/swarm.yml` |

## Signal

Objet mince. Champs optionnels. Absence ≠ invention.

## Synapses / états

`DECLARED` ≠ `CONNECTED` ≠ `ACTIVE` ≠ `LIVE_VERIFIED`

LIVE_VERIFIED = Carl seulement. Un 200 n'est pas VERT. Architecture prête ≠ réseau connecté.

## Routage

Capacité pertinente = candidat. Jamais autorité.

## Conscience

CLAIM, jamais `consciousness=true`.

## Limites réelles

Cette couche formalise la circulation. Elle n'allume pas Gemini, OpenRouter ou xAI.
Helpers : `sdk/nerve.js`. Schéma : `schema/nerve.v0.json`.
