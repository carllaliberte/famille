# Formal layer — honest

This is not Coq. This is not Lean. This is not EasyCrypt.
A missing machine-checked theory keeps the label honest.

What ships today, and is already formal enough to compose:

| Primitive | Status |
|---|---|
| JSON schema | contract of the claim |
| judge (`juger`) | total decision procedure |
| INTERDIT | negative specification |
| UFHY1 AND combiner | both signatures verify today |
| ε-budget | composable security, ε = 0 refused |
| MODE | classical by default |
| DOSSIER | composition root |

## What would be proven later

If a mechanized theory is added, these are the claims — not theorems today:

1. UFHY1 (Ed25519 ∧ ML-DSA-65) is EUF-CMA if at least one component is.
2. The leftover-hash extractor bound holds for the declared Hmin and ε.
3. MODE `quantique` is reachable only when every physical gate holds.

Tools named, not used: EasyCrypt / EASYPQC, PQ-CryptoVerif, Jasmin.
Do not write « formally verified » until one of those proofs lives in this cadastre.

The only bug is a lying label.
