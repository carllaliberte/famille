# Omni-Core — future-proof organism, not a second brain

Acorn is built around **capabilities**, not a model, a company, a cloud, a GPU,
a QPU, an API, or a generation of hardware.

```
IDENTITY ≠ MODEL ≠ CHANNEL ≠ CAPABILITY ≠ AUTHORITY
CAPABILITY ≠ INTELLIGENCE ≠ COMPUTE ≠ AUTHORITY
```

Four independent axes. The first three may grow. The fourth does not grow
because a new machine appeared.

```
HUMAN
  │
BREAKER
  │
ACORN CORTEX
  │
OMNI-CORE (this contract)
  ├── intelligence
  ├── compute
  ├── connector / flux (informational membrane)
  └── physical observation
```

This is **not** a 7th chantier, a second Cortex, a super-organism, or a
collective consciousness. Cortex remains maître d’œuvre. Omni-Core is the
horizon contract that keeps the organism open to unknown machines and
unknown intelligences without granting them authority.

## Honest states

`DEFINED ≠ DISCOVERED ≠ CONNECTED ≠ MEASURED ≠ EXECUTABLE ≠ VERIFIED ≠ LIVE`

Unmeasured = `UNKNOWN`. Never `READY` / `LIVE` / `AVAILABLE` / `CERTIFIED`
without proof. A future paradigm (photonic, neuromorphic, analog, …) is
`UNKNOWN_CAPABILITY` until it is measured. It is not forced into GPU or QPU.

## Invariants the swarm cannot rewrite

- CAPABILITY ≠ AUTHORITY
- HUMAN AUTHORITY
- BREAKER = HUMAN (outside the optimization loop)
- MERGE = HUMAN
- NO SECRET EXFILTRATION
- NO FALSE PROOF
- NO UNMEASURED LIVE CLAIM
- NO SILENT AUTHORITY ESCALATION
- PROVENANCE REQUIRED
- OBSERVED ≠ PROPOSED
- DEFINED ≠ EXECUTED
- DETECT ≠ FIX
- OBSERVE ≠ ACT
- TWIN ≠ REAL

## Runtime

```bash
node scripts/acorn-omni-core.mjs
```

Prints an honest Cortex view: axes, unknown capabilities, diagnostic
(detected, not fixed), digital twin (not applied), local compute proof,
QPU held, breaker denied, constitution denied.

Schema: [`schema/omni-core.v0.json`](schema/omni-core.v0.json).
Contract: [`scripts/acorn-omni-core.mjs`](scripts/acorn-omni-core.mjs).
Registry: [`CAPABILITY.md`](CAPABILITY.md).

UN GRAND CHANTIER N'EST PAS TERMINÉ PARCE QUE LE CODE EXISTE.
DEFINED ≠ CODE VERIFIED ≠ TEST VERIFIED ≠ EXECUTED ≠ MEASURED ≠ LIVE VERIFIED.

The swarm may grow. The Cortex may learn. The compute fabric may expand.
Human authority remains above the system.
