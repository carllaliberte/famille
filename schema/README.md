# Schéma juge v0

Mêmes clés que `peut_dire_quantique`.

```bash
python -m quantum peut-dire --fichier carte.json
```

exit 0 = les quatre tiennent. exit 2 = classique.
Hôte : https://acorn-royal-dune-blend.grok.me
