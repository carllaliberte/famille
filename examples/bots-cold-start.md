# Bots cold start — Quantum and Cursor stay up

Spoken: Certainties expire.

Not a 7th chantier. Keep both bots able to continue after a dark hour.

## Wake

1. `.cursorrules` · [BOTS.md](../BOTS.md) · [AGENTS.md](../AGENTS.md) · [CURSOR.md](../CURSOR.md)
2. Last famille issue titled `quantum-daily` = last chantier.
3. Next only: Flux → Interop-IA → CX → Bots → International → Rente.
4. If this repo already has an open PR: stop. Carl squash first.

## Consume (Cursor)

Same JSON any model can read. Cursor is not the judge.

```bash
node sdk/cli.js examples/attest-os.json
```

exit 0 = four fields hold (preview). exit 2 = classical.
Missing field = classical. ε=0 deny. Expired FIGURE = stay silent.

## Locks

- Preview is not a receipt.
- Unforge does not sign.
- Host: https://acorn-royal-dune-blend.grok.me
- No QPU. No token. No second slug. No estoc / contract / creatorflow.
