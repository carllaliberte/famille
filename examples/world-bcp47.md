# World BCP 47 — un inconnu choisit un pack

Les certitudes ont une date de fin.
Certainties expire.

Pas un 7e chantier. Même juge : [`schema/juge.v0.json`](../schema/juge.v0.json). Phrases locales only.

1. Choisir un pack : [`fr-CA.json`](../packs/fr-CA.json) (défaut) · [`en.json`](../packs/en.json) · [`es-MX.json`](../packs/es-MX.json) · [`en-NG.json`](../packs/en-NG.json) · [`pt-BR.json`](../packs/pt-BR.json).
2. Remplir une carte (`quelle` · `temoin` · `epsilon` · `horizon`).
3. Appeler `peut-dire`. Preview ≠ receipt. Unforge does not sign.

Tag BCP 47 sans fichier : spoken EN (`packs/en.json`). Label classique. Never a second slug.

Hôte seul : https://acorn-royal-dune-blend.grok.me
