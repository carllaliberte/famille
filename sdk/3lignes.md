# SDK 3 lignes

```js
import { peutDire } from './peut-dire.js'
const v = peutDire({ quelle:'os', temoin:'aucun', epsilon:1e-6, horizon:'2027-12-31' })
console.log(v.mode, v.preview, v.phrase)
```

Any model. Same JSON. Preview, not a receipt. No npm scope required.
