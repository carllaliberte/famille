import test from"node:test";import assert from"node:assert/strict";import{createExperiment,simulateExperiment,comparePrediction,learnExperiment}from"../scripts/acorn-experimentation-digital-twin.mjs";test("experiment",()=>assert.equal(createExperiment({hypothesis:"x"}).authority,false));test("simulation has no effect",()=>assert.equal(simulateExperiment({experiment:createExperiment({})}).external_effect,false));test("comparison",()=>assert.equal(comparePrediction({predicted:[1],observed:[1]}).matches,true));test("learning gated",()=>assert.equal(learnExperiment({comparison:{measured:true},verified:false}).learned,false));