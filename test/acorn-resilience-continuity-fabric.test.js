import test from "node:test";import assert from "node:assert/strict";import {assessResilience,recover,learnRecovery,assertResilienceConstitution} from "../scripts/acorn-resilience-continuity-fabric.mjs";test("coverage measured",()=>assert.equal(assessResilience({services:["a","b"],failures:["a"]}).coverage,.5));test("recovery bounded",()=>assert.equal(recover({plan:{},failed:["a"]}).external_effect,false));test("learning requires verification",()=>assert.equal(learnRecovery({assessment:{measured:true},verified:false}).learned,false));