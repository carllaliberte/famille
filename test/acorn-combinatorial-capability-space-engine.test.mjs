import assert from"node:assert/strict";import{runCombinatorialCapabilitySpace,assertCombinatorialCapabilitySpaceConstitution}from"../scripts/acorn-combinatorial-capability-space-engine.mjs";const r=runCombinatorialCapabilitySpace({capabilities:[{id:"a"},{id:"b"},{id:"c"}],unknowns:["u"],maxDepth:3,maxKeep:4});assert.equal(r.state,"CAPABILITY_SPACE_NAVIGABLE");assert.equal(r.measured.pair_space,3);assert.equal(r.diversified.candidates.length<=4,true);assert.equal(assertCombinatorialCapabilitySpaceConstitution({auto_execute:true}).valid,false);