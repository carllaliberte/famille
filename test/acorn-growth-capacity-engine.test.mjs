import assert from"node:assert/strict";import{runGrowthCapacityEngine,assertGrowthCapacityConstitution,compoundVerifiedLearning}from"../scripts/acorn-growth-capacity-engine.mjs";const r=runGrowthCapacityEngine({capabilities:[1,2],experiments:[1],gaps:["g"]});assert.equal(r.state,"GROWTH_CAPACITY_LOOP_READY");assert.equal(r.authority,false);assert.equal(r.levers.length>0,true);assert.equal(assertGrowthCapacityConstitution({auto_execute:true}).valid,false);assert.equal(compoundVerifiedLearning({outcomes:[{measured:true,verified:true,evidence:["e"]}]}).reuse_count,1);