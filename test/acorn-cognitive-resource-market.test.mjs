import assert from"node:assert/strict";import{buildCognitiveResourceMarket,evaluateCognitiveResourceMarket,assertCognitiveResourceMarketConstitution}from"../scripts/acorn-cognitive-resource-market.mjs";const r=buildCognitiveResourceMarket({items:[{id:"x"}]});assert.equal(r.items.length,1);assert.equal(r.authority,false);assert.equal(evaluateCognitiveResourceMarket({candidate:r.items[0],measurement:{measured:true,verified:true,evidence:["e"],score:1}}).reusable,true);assert.equal(assertCognitiveResourceMarketConstitution({auto_execute:true}).valid,false);