import test from "node:test";import assert from "node:assert/strict";import {createPlan,assertConstitution} from "../scripts/acorn-value-optimizer-20260918.mjs";test("plan remains governed",()=>{const p=createPlan({scope:"value optimizer"});assert.equal(p.state,"PROPOSED");assert.equal(p.requires_authorization,true);});test("constitution",()=>{assert.equal(assertConstitution(),true);assert.throws(()=>assertConstitution({breaker_touched:true}),/BREAKER/);});