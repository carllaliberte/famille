import assert from"node:assert/strict";import{runExpansionCompiler,assertExpansionCompilerConstitution,versionVerifiedExpansion}from"../scripts/acorn-expansion-evolution-compiler.mjs";const r=runExpansionCompiler({capabilities:[{id:"a"}],candidates:[{id:"x"}]});assert.equal(r.authority,false);assert.equal(r.plans.length,1);assert.equal(assertExpansionCompilerConstitution({auto_execute:true}).valid,false);assert.equal(versionVerifiedExpansion({plan:r.plans[0],measurement:{measured:true,verified:true},evidence:["e"]}).verified,true);