import assert from"node:assert/strict";import{runRecursiveExpansionOrchestrator,assertRecursiveExpansionOrchestratorConstitution}from"../scripts/acorn-recursive-expansion-orchestrator.mjs";const r=runRecursiveExpansionOrchestrator({capabilities:[{id:"a"},{id:"b"},{id:"c"}],signals:["u"],budget:5});assert.equal(r.authority,false);assert.equal(r.frontier.length>0,true);assert.equal(r.frontier.length<=5,true);assert.equal(assertRecursiveExpansionOrchestratorConstitution({auto_execute:true}).valid,false);