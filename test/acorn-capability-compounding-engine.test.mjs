import assert from"node:assert/strict";import{runCapabilityCompounding,promoteVerifiedCompound,assertCapabilityCompoundingConstitution}from"../scripts/acorn-capability-compounding-engine.mjs";const r=runCapabilityCompounding({capabilities:[{id:"a"},{id:"b"},{id:"c"}],maxDepth:3});assert.equal(r.state,"COMPOUNDING_READY");assert.equal(r.frontier.candidates.length>0,true);assert.equal(r.authority,false);assert.equal(promoteVerifiedCompound({candidate:{id:"x"},measurement:{measured:true,verified:true},evidence:["e"]}).reusable,true);assert.equal(assertCapabilityCompoundingConstitution({auto_execute:true}).valid,false);