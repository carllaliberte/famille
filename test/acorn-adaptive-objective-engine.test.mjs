import assert from"node:assert/strict";import{buildAdaptiveObjectiveEngine,evaluateAdaptiveObjectiveEngine,assertAdaptiveObjectiveEngineConstitution}from"../scripts/acorn-adaptive-objective-engine.mjs";const r=buildAdaptiveObjectiveEngine({items:[{id:"x"}]});assert.equal(r.items.length,1);assert.equal(r.authority,false);assert.equal(evaluateAdaptiveObjectiveEngine({candidate:r.items[0],measurement:{measured:true,verified:true,evidence:["e"],score:1}}).reusable,true);assert.equal(assertAdaptiveObjectiveEngineConstitution({auto_execute:true}).valid,false);