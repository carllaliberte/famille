import assert from"node:assert/strict";import{runCortexBrainCycle,assertCortexBrainConvergence}from"../scripts/acorn-cortex-brain-convergence.mjs";const r=runCortexBrainCycle({task:{id:"t"},hypotheses:[{id:"a",label:"A",prior:.5},{id:"b",label:"B",prior:.5}]});assert.equal(r.authority,false);assert.equal(r.auto_execute,false);assert.equal(assertCortexBrainConvergence({auto_execute:true}).valid,false);assert.ok(Array.isArray(r.gaps));