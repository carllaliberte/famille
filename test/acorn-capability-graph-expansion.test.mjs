import assert from"node:assert/strict";import{buildCapabilityGraphExpansion,evaluateCapabilityGraphExpansion,assertCapabilityGraphExpansionConstitution}from"../scripts/acorn-capability-graph-expansion.mjs";const r=buildCapabilityGraphExpansion({items:[{id:"x"}]});assert.equal(r.items.length,1);assert.equal(r.authority,false);assert.equal(evaluateCapabilityGraphExpansion({candidate:r.items[0],measurement:{measured:true,verified:true,evidence:["e"],score:1}}).reusable,true);assert.equal(assertCapabilityGraphExpansionConstitution({auto_execute:true}).valid,false);