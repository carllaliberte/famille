import assert from"node:assert/strict";import{buildCollectiveHypothesisEngine,evaluateCollectiveHypothesisEngine,assertCollectiveHypothesisEngineConstitution}from"../scripts/acorn-collective-hypothesis-engine.mjs";const r=buildCollectiveHypothesisEngine({items:[{id:"x"}]});assert.equal(r.items.length,1);assert.equal(r.authority,false);assert.equal(evaluateCollectiveHypothesisEngine({candidate:r.items[0],measurement:{measured:true,verified:true,evidence:["e"],score:1}}).reusable,true);assert.equal(assertCollectiveHypothesisEngineConstitution({auto_execute:true}).valid,false);