import test from "node:test";import assert from "node:assert/strict";import {measureValue,compareOffers,learnValue,assertValueConstitution} from "../scripts/acorn-value-economic-fabric.mjs";test("margin measured",()=>assert.equal(measureValue({revenue:10,cost:4}).margin,6));test("learning requires verification",()=>assert.equal(learnValue({measurement:{measured:true},verified:false}).learned,false));test("no auto spend",()=>assert.equal(assertValueConstitution().auto_spend,false));