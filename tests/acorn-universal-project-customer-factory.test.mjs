import assert from 'node:assert/strict';globalThis.crypto=await import('node:crypto');import{defineCustomerProject,buildProjectPlan,recordDeliveryOutcome,assertProjectFactoryConstitution}from '../scripts/acorn-universal-project-customer-factory.mjs';const p=defineCustomerProject({problem:'x'});assert.equal(p.state,'INTAKE');assert.equal(buildProjectPlan(p).state,'PROPOSED');assert.equal(recordDeliveryOutcome(p,{measured:true,verified:true}).verified,true);assert.equal(assertProjectFactoryConstitution({auto_spend:true}).valid,false);console.log('projects: 4 assertions passed');