import assert from 'node:assert/strict';import{discoverUnknowns,buildLearningExperiment,learnFromOutcome,assertAdaptiveLearningConstitution}from '../scripts/acorn-adaptive-discovery-learning.mjs';assert.equal(discoverUnknowns({known:['a'],gaps:['b']}).unknowns[0],'b');assert.equal(buildLearningExperiment({hypothesis:'h'}).simulation_required,true);assert.equal(learnFromOutcome({outcome:{measured:true,verified:true,id:'1'}}).state,'LEARNED');assert.equal(assertAdaptiveLearningConstitution({auto_adopt:true}).valid,false);console.log('learning: 4 assertions passed');