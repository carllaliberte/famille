import assert from 'node:assert/strict';import{buildAutonomyCycle,requestAutonomyAction,assertAutonomyConstitution}from '../scripts/acorn-continuous-cognitive-autonomy.mjs';const c=buildAutonomyCycle({options:[{id:'x',score:1}],outcomes:[{measured:true,verified:true}]});assert.equal(c.state,'NEXT_GOVERNED_STEP');assert.equal(requestAutonomyAction(c).requires_human_authorization,true);assert.equal(assertAutonomyConstitution(c).valid,true);assert.equal(assertAutonomyConstitution({auto_execute:true}).valid,false);console.log('autonomy: 4 assertions passed');