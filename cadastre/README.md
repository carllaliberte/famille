# Cadastre — seven obligations

Not seven scripts. One lattice.

| Rail | Obligation | Fail |
|---|---|---|
| `ufhy1_ietf.py` | M′ + OID 1.3.6.1.5.5.7.6.48 or label `ufhy1-maison` | signing raw M as composite RFC |
| `cbom.py` | every secret has QUELLE + HORIZON + named suite | `os` relabeled `qrng` |
| `drill.py` | seconds to reseal live dossiers | a drill that touches no file |
| `temoin_aaguid.py` | AAGUID + attestation, else MODE classique | invented YubiKey id |
| `sct.py` | signed inclusion + gossip of tree heads | calling the log a chain |
| `politique.py` | sortir is a conjunction; missing card = deny | allow on silence |
| `anti_theatre.py` | INTERDIT on our own films | cryostat without TÉMOIN |

```bash
python3 sept.py
```

FIPS 204 driver absent on the authoring box. We do not emit `id-MLDSA65-Ed25519-SHA512` signatures here. M′ encoding is implemented. The IETF label waits for the driver.
