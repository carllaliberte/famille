# Interdit — sept obligations

1. Label `ufhy1.v0` (raw message) as `id-MLDSA65-Ed25519-SHA512`.
2. Write the IETF OID without a FIPS 204 driver.
3. Relabel `os` as `qrng` in a CBOM.
4. A drill that passes without touching a file.
5. Invent a YubiKey AAGUID. Empty allowlist = MODE classique.
6. Call REGISTRE a blockchain. An SCT is not a block.
7. `sortir=allow` if a card is missing.
8. Cryostat / QPU / chandelier on screen without a TÉMOIN card.
9. Token, L1, quantum coin.
10. ε = 0.
