# ACORN — CONTINUOUS CORTEX CONDUCTOR
This conductor is the integration point for the existing Knowledge Fabric, Self-Optimization Engine and Universal AI Federation. It is intentionally a conductor, not a competing runtime or a new authority layer.

## Continuous cognition
OBSERVE → INGEST → VALIDATE → CONNECT → ROUTE → COMPOSE → PROPOSE → AUTHORIZE → EXECUTE → MEASURE → COMPARE → LEARN → REUSE → RECOMPOSE.

## What the cortex does
- detects missing evidence and capability gaps;
- identifies unmeasured outcomes;
- coordinates knowledge routing with available participants;
- selects the next measurable work;
- feeds measured outcomes back into future composition and optimization;
- exposes ecosystem health as an observed state.

## Cortex principle
The cortex coordinates the environment; it does not own the environment. Knowledge circulation requires consent where applicable. Execution remains separately authorized.

## Continuous operation
The conductor is safe to invoke repeatedly. Each cycle is observable and deterministic from its inputs. It never treats an unmeasured hypothesis as truth.

## Non-negotiable invariants
CAPABILITY ≠ AUTHORITY.
LEARNING ≠ AUTHORITY.
PROPOSAL ≠ EXECUTION.
PAYMENT ≠ EXECUTION.
AUTO-MERGE = FALSE.
AUTO-SPEND = FALSE.
AUTO-SIGNATURE = FALSE.
BREAKER = UNTOUCHED.
