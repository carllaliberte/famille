# Combinatorial Capability Space Engine

Contract: `acorn.combinatorial-capability-space-engine.v1`.

Measures and navigates the rapidly expanding space produced by capability composition. It expands candidates, bounds the search, diversifies exploration and routes candidates toward governed experiments. Search-space size is never treated as proof.

This is a continuation of the recursive growth architecture, not a parallel Cortex or execution system.