# ACORN — CORTEX KNOWLEDGE FABRIC

Les certitudes ont une date de fin.

This file describes a local rail. It is not a receipt. It is not LIVE. It is not `claim.v0`.

Acorn can coordinate shared environmental cognition: knowledge, capabilities, patterns, constraints, risks and outcomes become discoverable and reusable. It does not become an authority above the parts.

## Core loop

OBSERVE → PROVENANCE → VALIDATE → SHARE → CONNECT → DERIVE → TEST → MEASURE → LEARN → REUSE.

These are functions. They are not a live run. `validateKnowledge` is a local gate. It does not execute, spend, merge, or sign.

## Evidence gate

`validateKnowledge` stays `PROVISIONAL` unless evidence tokens are present. Empty evidence throws `EVIDENCE_REQUIRED`. A count of tokens is not a measured proof. This rail therefore does not mint `VERIFIED` from `evidence.length`. Presence of tokens is `OBSERVED`. `VERIFIED` remains a named state, unused by this gate.

The object `id` is FNV-1a 32 (`id_algo: fnv1a-32`) of subject|predicate|value|source|scope|type. That fingerprint is not SHA-256 and not `claim.v0` `evidence_hash`. LU still needs the hash of the content actually read. This rail does not mint that hash.

`expires_at` in the past, or `state: EXPIRED`, cannot be re-validated, routed, graphed, or shared as current. `expireKnowledge` is not the only reader of the date.

## Transfer

`transferKnowledge` runs an in-process A → B path. Provenance is preserved. Authority does not move. `path_ran` is not EXECUTED of the grand chantier.

`assertCortexConstitution` without a snapshot throws. Flags are not optional. A transfer object that actually ran A → B is required. Flags alone are not a constitution.

## Collective intelligence

Different intelligences and organizations can contribute knowledge without surrendering ownership or authority. Compatible items connect through an undirected graph. Routing prefers confidence. Expired knowledge is downgraded, never silently treated as current.

## Completeness

DEFINED ≠ CODE VERIFIED ≠ TEST VERIFIED ≠ EXECUTED ≠ MEASURED ≠ LIVE VERIFIED.

Local tests exercise the rail. They are not EXECUTED in production. They are not a live measure. Carl = MERGE.

Carte juge / epsilon / horizon / témoin absents sur ce geste → MODE classique. Pas comblé.

Pas de mesure JSON ni date d'ancrage. Trou nommé. Pas comblé.

COGNITIVE FABRIC reste DECLARED (AGENTS / Quantum). Pas CONNECTED. Pas LIVE.

FILE.md daté 2026-09-14 reste périmé. Pas de PR FILE tant que #1007 n'est pas mergée.

## Hard invariants

No hidden learning, no authority escalation, no automatic signature/spend/merge, no Breaker access.
