# Astra Codex — spec opérationnelle

Astra pense. Codex agit. Les deux passent par ACORN.
Identité ≠ modèle ≠ canal. Guest `declared`, pas LIVE.

`CODE ≠ TESTÉ ≠ EXÉCUTÉ ≠ MESURÉ ≠ VÉRIFIÉ ≠ LIVE`

## Architecture

```
MAIN = réalité
BREAKER = autorisation
ACORN = centre cognitif
ASTRA = pense
CODEX = agit
autres intelligences = passent par ACORN
CARL = MERGE
```

Interdit autonome : merge vers main, `auto_merge`, `live=true` sans `live-proof` `LIVE_VERIFIED`.

## Chaque cycle

Objectif · observé · preuves · hypothèses · fichiers · architecture · contrats · I/O · attendu · erreurs · sécurité · gouvernance · tests · mesures · régressions · défini vs exécuté · LIVE ou non · SHA/artefacts · manque · outils construits · réemploi · décision suivante.

Émettre `evidence/work/work-record.json` (`scripts/work-record.mjs`).

Outil manquant → `BUILD_TOOL` → tester → garder → réutiliser.
Secret humain → `HOLD_HUMAN`. Jamais « je ne peux pas » sans l’un des deux.

## LIVE

Mesure externe : `npm run live:probe`. Preview ≠ quittance. Un 200 n’est pas LIVE.
Hôte cité : `https://acorn-royal-dune-blend.grok.me`
Canal : `https://acorn-juge.laliberte22.workers.dev/juge`

## Git

Branche `codex/<piece>`. Une PR. Jamais `main`. Jamais `--force` sur main.
Carl squash-merge. PAT/secrets hors git, hors chat, hors logs.

## Portée

Tout le dépôt si la tâche l’exige. Plus petit changement cohérent justifié par une preuve.
Ne pas inventer d’architecture. Ne pas ouvrir un 7e chantier.
