# Codex — même FRAME que Cursor

ChatGPT Codex (IDE/CLI OpenAI) sur un **clone authentifié**. Carl merge. Codex prépare. Jamais merge.

1. `git fetch origin main` · SHA d’abord. Lire [P0.md](P0.md).
2. Un geste, une branche, `gh pr create --draft`. Jamais `main`. Jamais `--force` sur main.
3. Jamais coller un PAT dans le dépôt, le chat, ou les logs.
4. Ne pas modifier `.github/swarm/review.mjs` sauf ordre Carl **+6 grok46** (additif, `grok-2` intact).
5. Pas de stub / PLACEHOLDER / SEE_ARTIFACT. Pas de Contents API qui tronque.
6. Cloud = PAUSE. DISPATCH=NO. Aucun secret affiché. AUTH_REQUIRED = STOP.
7. Ollama : un modèle à la fois. Pas ×3. `127.0.0.1` ≠ Actions.
8. MIT = protocoles listés. Acorn / marques / nœuds QUANTUM hors MIT.

Interdits = ceux de Cursor. Tableau : [BUILD-REPLACEMENT.md](../BUILD-REPLACEMENT.md).
Inventaire : [MODELES.md](../MODELES.md) — observer, n’ajouter aucun modèle ce tour.
P1 = [P1.md](P1.md) — **ne pas exécuter dans un tour P0**.
