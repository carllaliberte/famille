# P1 — Contrat + tests morts

**À ne PAS exécuter dans le tour P0.** File après squash Carl du pack remplacement.

Objectif : REAL=1 seulement si HTTP 2xx et corps non vide.
Mock 400 / secret vide / Ollama absent = skip ou REAL=0. CI verte sans daemon et sans secrets.

Déjà sur main (#429) : `scripts/siege.mjs` + `test/siege.test.js`.
Ne pas réécrire si les assertions tiennent. Ne pas toucher `review.mjs`.
Ne pas pull de poids. Ne pas parallèle ×3. Ne pas dispatcher.

Une PR. Titre = le geste. Carl merge.
