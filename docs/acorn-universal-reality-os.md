# ACORN — Universal Reality Operating System

Contract: `acorn.universal-reality-os.v1`.

This is a convergence fabric, not a second runtime. It unifies the existing project, value, quality, connection, intelligence, commercial, maintenance and evolution contracts around one permanent loop:

`HUMAN INTENT → OBSERVE → DISCOVER → COMPARE → COMPOSE → SIMULATE → QUALIFY → OFFER → ORDER → AUTHORIZE → EXECUTE → VERIFY → DELIVER → MEASURE → BENCHMARK → LEARN → REGISTER → REUSE → RECOMPOSE → OBSERVE`

## Maximum convergence

Every project can produce reusable capability; every verified capability can reveal opportunities; every measured outcome can improve routing and composition; every expired assertion returns to observation.

## Truth

`DEFINED ≠ CODE_PRESENT ≠ TESTED ≠ EXECUTED ≠ MEASURED ≠ VERIFIED ≠ LIVE`.

`BEST_KNOWN_IN_SCOPE` is scoped and evidence-based. It is never silently promoted to global optimum.

## Sovereignty

Capabilities, learning and optimization never grant authority. Money, contracts, signing, deletion, publication, protected writes and merge remain human/server controlled. No automatic spending, contracting or merging.

## Economic compounding

`PROJECT → OUTCOME → EVIDENCE → CAPABILITY → REUSE → PRODUCT → OPPORTUNITY → PROJECT`.

Value is measured separately from revenue; payment is not execution; execution is not customer value.
