# /attest errors

English (buyer). Default is classical.

| HTTP | When | Phrase |
|---|---|---|
| 400 | `epsilon` is 0, none, iid, missing | Error margin zero is a lie |
| 400 | `horizon` is UFHY1, a slogan, or a past date | Guarantee end date must be a future calendar day |
| 400 | `quelle` missing / unknown | Where the bits came from is missing |
| 200 | `quelle=os` | Classical — phone entropy is not quantum |
| 200 | qkd without `di` + transcript | Classical — no witness |
| 200 | four cards hold | Preview allow — not a receipt |

Never 200 with ε=0.
Host does not sign. Badge says PREVIEW.
