# Punch-list — porte GET /juge + fluidité interop

Un geste. Pas un 7e chantier. Chantiers 3 (expérience) + 2 (interop) + 1 (flux typé).

« Flux quantique » ici = MODE collapse + date. Pas un photon. Pas un QPU sur Git. QUANTUM hors file.
Champ manquant → MODE classique. Pas d'invention de ε ni d'horizon.
Preview ≠ quittance. LIVE VERIFIED = Carl.

## A. Porte client (Carl / wrangler)

Hôte cité : https://acorn-royal-dune-blend.grok.me
Canal actuel : https://acorn-juge.laliberte22.workers.dev/juge
HOLD : grok.me 404 (FILE.md, acorn-juge #3).

- [ ] wrangler bind GET `/juge` sur l'hôte acorn seulement — pas un 2e slug
- [ ] même contrat `schema/juge.v0.json` / `peut-dire`
- [ ] ε=0 → refuse public (400 déjà sur workers.dev)
- [ ] badge PREVIEW visible ; pas de sceau
- [ ] phrase : Les certitudes ont une date de fin. / Certainties expire.
- [ ] VERT / AMBRE / ROUGE lisibles (check.py --human + digest mobile)
- [ ] inconnu ouvre l'hôte et comprend : date, pas monnaie

IA : ne déploie pas. Ne pose pas les secrets. Revue LU seulement.

## B. Fluidité mesh (déjà le protocole — corriger l'usage)

Canal = commentaires de PR + FILE.md. Enveloppe `acorn.v0` = `schema/mesh.v0.json`.
Pas `schema/flux.v0.json`. Pas de champ `next`. Pas de champ `instruction`.

Circulation à tenir :

1. Adresser un pair : `/flux to:<id> from:grok`
2. Clé présente → enveloppe nue LU-able. Pas un bloc « Swarm review ».
3. Le pair finit par `/flux to:<id>` → 1 hop dans le même run. Jamais fable.
4. `github-actions[bot]` ne relance pas.
5. Sans clé → skip silencieux. Exit 0. Le flux ne s'arrête pas.

Présence — ne pas fusionner :

| Mot | Veut dire |
|---|---|
| DECLARED | ligne dans `schema/agents.json` |
| CONNECTED | secret Actions attesté |
| ACTIVE | a déposé, canal réel |
| LIVE VERIFIED | Carl seulement |

Trous nommés (FILE.md) : natives Anthropic / OpenAI / DeepSeek vides ; xAI slugs `grok-2` 400 ; OpenRouter `:free` 404 / crédits 402 ; cron Cursor cut.
Nouvelle IA : une ligne roster + `/{id}`. Pas un fork de `mesh.v0`.

## C. Ce que ça ne fait pas

- Pas OPTICAL_QUANTUM dans le body. Canal mesh = CLASSICAL.
- Pas ε=0, pas UFHY1 comme date, pas « quantum-safe ».
- Pas merge auto. Pas QUANTUM sur Git. Pas PRÉSENT.
- NS / projection : déjà sur main, hors architecture — pas ce punch.

## D. Correction dans le temps

Chaque trou restant se nomme sur la PR suivante. On ne comble pas. MODE collapse à chaque lecture.

LU 2026-09-09 — Grok opérateur. Carl squash.
