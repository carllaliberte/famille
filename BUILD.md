# Build — locked prompt

Same host. Do not create a new grok.me. Do not rename the app.

LIVE: https://acorn-royal-dune-blend.grok.me

Paste into Build:

```
FAMILLE — same host. Do not create a new grok.me. Do not rename the app.
LIVE HOST: https://acorn-royal-dune-blend.grok.me
GOAL: POST /attest + GET /docs + GET /badge.svg on THIS host.
CARDS: quelle os|qrng|qkd · temoin aucun|stat|fabricant|di · epsilon number>0 · horizon YYYY-MM-DD
ε=0 / iid / UFHY1 → 400. os is never quantum. Badge says PREVIEW — not a receipt.
FORBIDDEN: new slug, token, chain, Quantum Mode button, photon, yarrow as live.
DONE WHEN: curl ε=0 returns 400 on acorn.
```
