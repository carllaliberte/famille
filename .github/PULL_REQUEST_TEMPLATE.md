## Résumé
- Capacités ajoutées (phases 1–5) :
- `AI_WRITE = DENIED` / `AI_MERGE = DENIED` : oui
- Preview ≠ receipt : oui
- epsilon > 0 : oui
- `juge.v0.json` / `flux.v0.json` intacts : oui
- ML-KEM : opt-in only, `PQC_NOT_PRESENT` si pas de module FIPS 203

## Tests
- [ ] `npm test` vert

## Human gate
Carl squash seulement. Pas de merge automatique. Pas de signature IA.
