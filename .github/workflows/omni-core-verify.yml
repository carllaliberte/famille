name: omni-core-verify
on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run omni-core contract
        run: node --test test/acorn-omni-core.test.js
      - name: Honest local proof loop
        run: node scripts/acorn-omni-core.mjs
