# ACORN — real Grok Build bridge.
# Trigger: Carl only, by an issue/PR comment beginning with /grok-build.
# The human intent is packaged once as an Acorn cognitive task before Grok runs.
# Architecture: AI Connector / Flux -> Global Breaker -> Acorn -> downstream intelligence.
# Grok Build runs headlessly in an isolated GitHub Actions workspace.
# It may edit the workspace, but never main, never merge, never auto-merge.
name: Grok Build Bridge

on:
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      prompt:
        description: "Grok Build task"
        required: true
        type: string
      ref:
        description: "Ref to work from"
        required: false
        default: main
        type: string

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: grok-build-bridge
  cancel-in-progress: false

jobs:
  build:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' &&
       github.event.comment.user.login == 'carllaliberte' &&
       github.event.comment.body != '' &&
       startsWith(github.event.comment.body, '/grok-build'))
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Global breaker
        env:
          ACORN_SYSTEM_MODE: ${{ vars.ACORN_SYSTEM_MODE }}
        run: node -e "const m=String(process.env.ACORN_SYSTEM_MODE||'RUN').toUpperCase(); if(m==='OFF') { console.error('GLOBAL_BREAKER_OFF'); process.exit(1) } console.log(JSON.stringify({mode:m,production_write_allowed:false,auto_merge:false,live:false,human_authority:'carl'}))"

      - name: Resolve source ref and prompt
        id: input
        env:
          EVENT_NAME: ${{ github.event_name }}
          COMMENT: ${{ github.event.comment.body }}
          DISPATCH_PROMPT: ${{ inputs.prompt }}
          DISPATCH_REF: ${{ inputs.ref }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = "issue_comment" ]; then
            PROMPT="${COMMENT#/grok-build}"
            PROMPT="${PROMPT# }"
            if [ -z "$PROMPT" ]; then
              echo "Grok Build: prompt vide" >&2
              exit 1
            fi
            if gh api "repos/${GITHUB_REPOSITORY}/pulls/${{ github.event.issue.number }}" >/tmp/pr.json 2>/dev/null; then
              REF=$(jq -r .head.sha /tmp/pr.json)
              TARGET_PR="${{ github.event.issue.number }}"
            else
              REF="main"
              TARGET_PR=""
            fi
          else
            PROMPT="$DISPATCH_PROMPT"
            REF="${DISPATCH_REF:-main}"
            TARGET_PR=""
          fi
          printf '%s' "$PROMPT" > /tmp/grok-original-intent.txt
          echo "ref=$REF" >> "$GITHUB_OUTPUT"
          echo "target_pr=$TARGET_PR" >> "$GITHUB_OUTPUT"

      - name: Checkout source
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.input.outputs.ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Create Acorn cognitive task
        id: task
        env:
          PROMPT_FILE: /tmp/grok-original-intent.txt
          SOURCE_REF: ${{ steps.input.outputs.ref }}
          TARGET_PR: ${{ steps.input.outputs.target_pr }}
          ACORN_SYSTEM_MODE: ${{ vars.ACORN_SYSTEM_MODE }}
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import { readFileSync, writeFileSync } from 'node:fs';
          import { createCognitiveTask, taskPrompt } from './.github/swarm/cognitive-task.mjs';
          const intent = readFileSync(process.env.PROMPT_FILE, 'utf8');
          const task = createCognitiveTask({
            intent,
            source: 'carl',
            channel: 'grok-build',
            ref: process.env.SOURCE_REF,
            capabilities: ['build', 'review'],
            context: process.env.TARGET_PR ? { pr: Number(process.env.TARGET_PR) } : null,
            env: process.env,
          });
          writeFileSync('/tmp/acorn-task.json', `${JSON.stringify(task, null, 2)}\n`);
          writeFileSync('/tmp/grok-prompt.txt', `${taskPrompt(task)}\n\nOriginal human intent:\n${intent}`);
          console.log(`task=${task.task} state=${task.state} ref=${task.provenance.ref}`);
          NODE
          echo "task_version=cognitive-task.v1" >> "$GITHUB_OUTPUT"

      - name: Install Grok Build
        run: |
          set -euo pipefail
          curl -fsSL https://x.ai/cli/install.sh | bash
          command -v grok
          grok --version

      - name: Verify xAI credential exists
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$XAI_API_KEY" ]; then
            echo "HOLD: XAI_API_KEY is not configured in GitHub Actions secrets."
            exit 2
          fi

      - name: Run Grok Build
        id: grok
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          PROMPT_FILE: /tmp/grok-prompt.txt
        run: |
          set +e
          {
            echo 'You are Grok Build operating as a cognitive/service node inside Acorn/FAMILLE.'
            echo 'The ACORN TASK below is the canonical task. Preserve its original human intent; do not translate it into a different objective.'
            echo 'Work directly in the checked-out repository and solve the requested task.'
            echo 'You may inspect and modify the working tree, but DO NOT commit, push, merge, or change main.'
            echo 'Return a concise factual report of what you inspected, what you changed, tests run, and remaining blockers.'
            echo 'Preserve Acorn governance: human authority is Carl; auto_merge=false; live=false.'
            echo 'Do not create or expose secrets. Do not modify protected semantic/protocol paths unless the task explicitly requires it and explain such changes.'
            echo 'Never claim execution you did not perform.'
          } > /tmp/grok-system.txt
          PROMPT="$(cat "$PROMPT_FILE")"
          SYSTEM="$(cat /tmp/grok-system.txt)"
          FULL_PROMPT="${SYSTEM}"$'\n\nTASK:\n'"${PROMPT}"
          grok -p "$FULL_PROMPT" --output-format streaming-json > /tmp/grok-output.jsonl 2>&1
          RC=$?
          echo "exit_code=$RC" >> "$GITHUB_OUTPUT"
          set -e
          tail -c 12000 /tmp/grok-output.jsonl > grok-build-output.txt || true
          if [ "$RC" -ne 0 ]; then
            echo "Grok Build failed with exit code $RC"
            exit "$RC"
          fi

      - name: Measure resulting diff
        id: diff
        run: |
          set -euo pipefail
          git status --short | tee grok-build-diff.txt
          git diff --stat | tee -a grok-build-diff.txt
          if git diff --quiet && [ -z "$(git status --porcelain)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Create isolated branch and PR
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_REF: ${{ steps.input.outputs.ref }}
        run: |
          set -euo pipefail
          git config user.name "acorn-grok-build[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          BRANCH="ai/grok-build-$(date -u +%Y%m%d-%H%M%S)"
          git checkout -b "$BRANCH"
          git add -A
          git commit -m "feat: Grok Build task"
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push origin "HEAD:${BRANCH}"
          BODY=$(cat <<EOF
          Real Grok Build execution from \`${SOURCE_REF}\`.

          - The original human intent was preserved in an Acorn cognitive task.
          - Grok Build ran headlessly in GitHub Actions.
          - Changes are isolated on \`${BRANCH}\`.
          - No merge was performed.
          - No auto-merge.
          - Human merge authority: Carl.
          - The workflow does not write \`main\` directly.
          EOF
          )
          gh pr create --base main --head "$BRANCH" --title "feat: Grok Build task" --body "$BODY"

      - name: Comment execution result
        if: always() && github.event_name == 'issue_comment'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NUMBER: ${{ github.event.issue.number }}
          RC: ${{ steps.grok.outputs.exit_code || 'not-run' }}
          CHANGED: ${{ steps.diff.outputs.changed || 'not-measured' }}
          TASK: ${{ steps.task.outputs.task_version || 'not-created' }}
        run: |
          set -euo pipefail
          STATUS="EXECUTED"
          if [ "$RC" != "0" ]; then STATUS="ERROR"; fi
          gh issue comment "$NUMBER" --body "Grok Build bridge: **$STATUS**. task=$TASK; exit=$RC; changed=$CHANGED. No merge performed. See workflow artifacts for the measured execution report."

      - name: Upload execution evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: grok-build-execution
          retention-days: 90
          path: |
            /tmp/acorn-task.json
            /tmp/grok-original-intent.txt
            grok-build-output.txt
            grok-build-diff.txt
          if-no-files-found: ignore
