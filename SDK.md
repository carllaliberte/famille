# Étage 3 — SDK

```js
import { juger } from '@famille/juge'
const v = await juger(carte) // {quantique, manques}
```

Même JSON que `peut-dire --fichier`. Pas un 2e juge. Pas npm tant que le contrat tient.
