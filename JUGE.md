# Juge universel

Une fonction. Quatre cartes. Deux sorties.
Pas une chaîne. Pas un consensus. Pas un vendor.

```
carte JSON  →  peut_dire_quantique  →  {quantique, manques, phrase}
                                          exit 0 | 2
```

## Cartes (le seul vocabulaire)

| Rail | Question | Refuse |
|---|---|---|
| QUELLE | d'où viennent les octets | `os` pour dire quantique |
| TÉMOIN | qui atteste le bit | `di` sans transcript |
| EPSILON | quelle marge | `0`, `none`, `iid` |
| HORIZON | jusqu'à quand | slogan UFHY1, date passée |

MODE n'est pas une 5e carte. MODE = le collapse.

## Trois étages (jamais mélangés)

1. **Vocabulaire** — repos publics (quelle, temoin, epsilon, horizon, mode, bruit).
2. **Juge** — `peut_dire` dans unforge. Pur. 0 réseau. 0 `data/`.
3. **Sceau** — nœud QUANTUM local. Hors Git. Hors grok.me.

L'hôte acorn **affiche** le jugement. Il ne signe pas.

## Consommateurs (même contrat)

```
CLI     python -m quantum peut-dire --fichier carte.json
CI      exit 0/2 dans un workflow
Preview formulaire acorn (badge PREVIEW)
SDK     plus tard, 3 lignes, même JSON
IA      Grok, Claude, autre — elles *appellent*, elles ne *sont* pas le juge
```

Universel = le schéma est public et n'importe qui peut implémenter
le même refus. L'implémentation de référence reste unforge.

## Attaques à nier (GARDE)

ε=0 · photon logiciel · di+simule · os relabel qkd · token · 2e slug · QUANTUM sur Git.
